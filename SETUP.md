# 🚀 Qaspilab - Запуск и использование

## ✅ Что уже настроено:

### 🎨 Техническая база
- **Next.js 14+** с App Router
- **Tailwind CSS v4** с кастомными цветами
- **shadcn/ui** компоненты (Button уже добавлен)
- **lucide-react** иконки
- **Framer Motion** анимации
- **next-intl** мультиязычность (3 языка: EN/RU/KK)
- **next-themes** поддержка тем

### 🌈 Цветовая схема
```css
--graphite: oklch(0.25 0.02 270);        /* Основной графитовый */
--violet: oklch(0.55 0.25 270);          /* Фиолетовый акцент */
--neon-blue: oklch(0.65 0.25 220);      /* Неоново-синий акцент */
```

### 📁 Структура
```
qaspilab/
├── app/
│   ├── [locale]/           ✅ Локализованные маршруты
│   │   ├── layout.tsx      ✅ Layout с Inter шрифтом
│   │   └── page.tsx        ✅ Главная страница с анимациями
│   ├── globals.css         ✅ Стили с кастомными цветами
│   └── layout.tsx          ✅ Корневой layout
├── components/
│   ├── ui/                 ✅ shadcn компоненты
│   ├── layout/             ✅ Header, Footer, Container
│   ├── animations/         ✅ FadeIn анимация
│   └── theme-provider.tsx  ✅ Поддержка тем
├── messages/               ✅ Переводы EN/RU/KK
├── lib/                    ✅ Утилиты и i18n настройки
└── middleware.ts           ✅ Роутинг локализации
```

## 🚀 Запуск проекта

```bash
# 1. Запуск dev сервера
npm run dev

# 2. Открыть в браузере
# http://localhost:3000 → автоматически перенаправит на /en
```

## 🌍 Доступные языки

- 🇺🇸 **English**: `/en`
- 🇷🇺 **Русский**: `/ru` 
- 🇰🇿 **Қазақша**: `/kk`

## 🎯 Следующие шаги для разработки:

### 1. Добавить секции лендинга
```bash
# Hero секция (уже есть базовая версия)
# About секция  
# Services секция
# Contact секция
```

### 2. Добавить больше shadcn компонентов
```bash
npx shadcn@latest add card
npx shadcn@latest add input
npx shadcn@latest add form
npx shadcn@latest add dialog
```

### 3. Настроить дополнительные страницы
```typescript
// app/[locale]/about/page.tsx
// app/[locale]/services/page.tsx
// app/[locale]/contact/page.tsx
```

### 4. Добавить контент в переводы
```json
// messages/en.json, ru.json, kk.json
{
  "about": { ... },
  "services": { ... },
  "contact": { ... }
}
```

## 🎨 Использование компонентов

### Анимации
```tsx
import FadeIn from '@/components/animations/fade-in';

<FadeIn delay={0.2} direction="up">
  <h1>Animated content</h1>
</FadeIn>
```

### Переводы
```tsx
import { useTranslations } from 'next-intl';

const t = useTranslations();
return <h1>{t('hero.title')}</h1>;
```

### Цвета
```tsx
className="bg-violet text-violet-foreground"
className="bg-neon-blue text-neon-blue-foreground"
className="bg-graphite text-graphite-foreground"
```

## 🛠️ Готово для продакшна

Проект полностью настроен и готов для:
- SEO оптимизации
- Добавления контента
- Форм обратной связи
- Интеграции с CMS
- Деплоя на Vercel/Netlify

---

**🎉 Успешной разработки лендинга Qaspilab! 🚀**